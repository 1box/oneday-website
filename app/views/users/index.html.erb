
<%= provide(:title, t(:all_users)) %>
<h1><%= t(:all_users) %></h1>

<%= will_paginate previous_label: t(:previous_label), next_label: t(:next_label) %>

<ul class="users">
  <% @users.each do |user| %>
    <%= render user %>
  <% end %>
</ul>

<%= will_paginate previous_label: t(:previous_label), next_label: t(:next_label) %>
